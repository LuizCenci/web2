<div class="container mt-5">
    <h2 class="card-title mb-4">Editar Usuário</h2>
    <form action="/usuarioUpdate" method="post">
        <input type="hidden" name="id" value="{{usuario.id}}">

        <div class="mb-3">
            <label for="login" class="form-label">Login:</label>
            <input type="text" class="form-control" id="login" name="login" value="{{usuario.login}}" required>
            <label for="senha" class="form-label">Senha:</label>
            <input type="password" class="form-control" id="senha" name="senha" value="{{usuario.senha}}" required>
            <label for="tipo" class="form-label">Tipo:</label>
            <input type="number" class="form-control" id="tipo" name="tipo" value="{{usuario.tipo}}" required>
        </div>

        <button type="submit" class="btn btn-warning">Editar</button>
    </form>
</div>
